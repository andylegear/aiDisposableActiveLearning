# Development Session Logs

This directory holds structured JSON logs for each AI-assisted development session.

## Naming Convention

```
sessions-{artifact_slug}.json
```

Each file is a JSON array of session objects.

Example: `sessions-01-unit-testing-gauntlet.json`

## Schema

Individual session entries conform to: `../schemas/dev-session.schema.json`
